# Winget installer manifest template.
# Po release goreleaserem vyplňte ${VERSION}, ${URL}, ${SHA256} a publikujte do
# https://github.com/microsoft/winget-pkgs.

PackageIdentifier: Animato.ClaudeHubDaemon
PackageVersion: ${VERSION}
InstallerType: zip
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: claude-hub-daemon.exe
    PortableCommandAlias: claude-hub-daemon
Commands:
  - claude-hub-daemon
ReleaseDate: ${RELEASE_DATE}
Installers:
  - Architecture: x64
    InstallerUrl: ${URL_WINDOWS_AMD64}
    InstallerSha256: ${SHA256_WINDOWS_AMD64}
ManifestType: installer
ManifestVersion: 1.6.0
